import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';
const ctx={window:{},console,Date};vm.createContext(ctx);vm.runInContext(fs.readFileSync('js/ibis-relationship-epistemics.js','utf8'),ctx);vm.runInContext(fs.readFileSync('js/ibis-foresight-engine.js','utf8'),ctx);const F=ctx.window.FTN.IbisForesight;assert(F);
const out=F.generate({now:'2026-09-07T12:00:00Z',opportunityMatches:[{opportunity:{id:'x',title:'Funding call',deadline:'2026-09-20',sourceUrl:'https://example.org',verifiedAt:'2026-09-07'},eligibility:'NOT_ASSESSED'}],relationships:[{id:'r1',type:'correlation',classification:'Official',confidence:'High',fromLabel:'A',toLabel:'B'}]});
assert.equal(out.success,true);assert.equal(out.probabilitiesEstimated,false);assert.equal(out.candidates.length,1,'Ordinary correlation must not become Foresight prediction');assert.equal(out.candidates[0].priority,'PREPARE_NOW');assert.equal(out.candidates[0].probability,null);assert.equal(out.candidates[0].eligibility,'NOT_ASSESSED');
const predictive=F.generate({now:'2026-09-07T12:00:00Z',relationships:[{id:'r2',type:'predictive',outOfSampleValidated:true,fromLabel:'A',toLabel:'B'}]});assert.equal(predictive.candidates.length,1);assert.equal(predictive.candidates[0].confidenceBasis,'PREDICTIVE_SIGNAL');assert.equal(predictive.candidates[0].probability,null,'Validated signal still must not invent a probability');
assert.equal(F.daysUntil('2026-09-11','2026-09-07T00:00:00Z'),5);assert.equal(F.priority(5),'ACT_NOW');
console.log('ibis Foresight Engine: explicit deadline/intent preparation, safe signal gating and no fabricated probabilities verified.');
