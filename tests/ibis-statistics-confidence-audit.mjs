import fs from 'node:fs';import vm from 'node:vm';import assert from 'node:assert/strict';
const ctx={window:{},console};vm.createContext(ctx);
for(const f of ['js/ibis-provenance.js','js/ftn-statistics.js','js/ftn-statistics-crime-adapter.js','js/ftn-statistics-fx-adapter.js','js/ibis-statistics-capability.js','js/ibis-confidence.js','js/ibis-statistics-confidence.js'])vm.runInContext(fs.readFileSync(f,'utf8'),ctx);
const FTN=ctx.window.FTN;const crime=JSON.parse(fs.readFileSync('data/crime-statistics.json','utf8'));const fx=JSON.parse(fs.readFileSync('data/fx-usd-ttd.json','utf8'));const catalog=FTN.IbisStatistics.buildCatalog({crime,fx});
const latest=FTN.IbisStatistics.query('What is the latest USD selling rate?',{catalog});const enriched=FTN.IbisStatisticsConfidence.enrich(latest,{now:'2026-09-07'});assert.equal(enriched.success,true);assert(enriched.confidence);assert.equal(enriched.confidence.sourceClass,'OFFICIAL_PRIMARY');assert.equal(enriched.confidence.claimType,'VERIFIED_FACT');assert.equal(enriched.confidence.probability,null);
const change=FTN.IbisStatistics.query('How much did murders change from 2023 to 2024?',{catalog});const changeEnriched=FTN.IbisStatisticsConfidence.enrich(change,{now:'2026-09-07'});assert.equal(changeEnriched.confidence.claimType,'DERIVED_CALCULATION');assert.notEqual(changeEnriched.confidence.level,undefined);
const unknown={success:true,source:{id:'new-unclassified-source'},provenance:{sourceIdentity:'Unknown',retrievalMethod:'PUBLIC_HTML',sourceReferenceDate:'2026-09-01'}};const u=FTN.IbisStatisticsConfidence.enrich(unknown,{now:'2026-09-07'});assert.equal(u.confidence.sourceClass,'UNKNOWN');assert.notEqual(u.confidence.level,'HIGH');
console.log('ibis Statistics Confidence: official-source classification, fact/derived distinction and unknown-source downgrade verified.');
