# DJ Name Drop Bank

Place DJ-owned audio drops here in the production app (WAV/MP3/M4A). The interface will expose hot buttons and the mix engine will trigger a selected drop on a phrase/beat boundary.

The production implementation must require user-supplied/licensed audio and must not extract audio from YouTube videos.
