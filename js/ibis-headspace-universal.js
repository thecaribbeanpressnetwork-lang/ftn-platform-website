// FTN Platform — final Headspace submit fallback.
// Specialist capture handlers execute first. This handler only receives requests no specialist
// claimed, then routes them through the persistent ibis runtime instead of showing prototype copy.
(function(global){
  'use strict';
  var form=document.getElementById('inputOrbit'),input=document.getElementById('headspaceQuery'),hint=document.getElementById('commandHint');
  if(!form||!input)return;
  function answerCard(){return document.querySelector('[data-thought="answer"]');}
  function renderWaiting(result){var F=global.FTN&&global.FTN.HeadspaceFabric,a=answerCard();if(F)F.focus(['answer','tools']);if(a){a.querySelector('.thought-bar>span').textContent='PERMISSION REQUIRED';a.querySelector('h2').textContent='ibis is ready to continue, but this action changes something outside Headspace.';var waits=result&&result.run&&result.run.result&&result.run.result.waitingPermissions||[];a.querySelector('p').textContent=waits.length?'Approval required for '+waits.map(function(x){return x.agent.toLowerCase();}).join(', ')+'.':'Approval is required before ibis can perform the external step.';}if(hint)hint.textContent='Execution paused at the permission boundary.';}
  function bestText(result){var outputs=result&&result.run&&result.run.result&&result.run.result.outputs||[];for(var i=outputs.length-1;i>=0;i--){var o=outputs[i].output||{},d=o.data||o.result||{};if(d.answer)return d.answer;if(o.result&&o.result.answer)return o.result.answer;if(typeof d==='string')return d;}return null;}
  function knownAnswer(text){if(/\b(who (invented|created|founded|owns)|inventor|creator)\b.*\bibis\b|\bibis\b.*\b(who (invented|created|founded|owns)|inventor|creator)\b/i.test(text))return'FTN ibis was created within FTN Platform as Caribbean-first intelligence infrastructure. It is an FTN-owned product; individual founder identity is not used as homepage marketing.';if(/\b(type|kind|name)\b.*\b(search|intelligence)\b|\bwhat is this (search|called)\b/i.test(text))return'This is intent-led, provenance-aware Caribbean intelligence: ibis connects a stated goal to source-backed opportunities, evidence and next actions.';if(/^\s*(what is|tell me about|explain)\s+(ftn\s+)?ibis\b/i.test(text))return'FTN ibis is a Caribbean intelligence app that connects source-backed opportunity data and FTN workflows to the web, browsers and compatible AI hosts through an MCP server and API.';return null;}
  function renderResult(result,route,text){var F=global.FTN&&global.FTN.HeadspaceFabric,a=answerCard();if(result&&result.status==='WAITING_PERMISSION'){renderWaiting(result);return;}if(F)F.focus(['answer']);if(a){a.querySelector('.thought-bar>span').textContent='IBIS';var txt=bestText(result)||knownAnswer(text);a.querySelector('h2').textContent=txt?'Here is what ibis can confirm.':'The general answer service is temporarily unavailable.';a.querySelector('p').textContent=txt||'Source-backed opportunity search and FTN routing remain available. Try an opportunity, grant, procurement or FTN service question, or sign in and try again shortly.';}if(hint)hint.textContent=(bestText(result)||knownAnswer(text))?'Answer completed.':'The language-model provider failed; ibis kept the failure visible and did not invent an answer.';}
  form.addEventListener('submit',async function(e){
    // This listener is intentionally registered last. Specialist handlers use capture +
    // stopImmediatePropagation; if one claimed the request, this fallback never runs.
    var text=input.value.trim();if(!text)return;e.preventDefault();e.stopImmediatePropagation();var FTN=global.FTN=global.FTN||{},token=FTN.HeadspaceRequestState&&FTN.HeadspaceRequestState.snapshot();if(hint)hint.textContent='ibis is assembling the faculties this request needs…';
    try{if(FTN.IbisRuntimeReady)await FTN.IbisRuntimeReady;if(!FTN.IbisRuntime)throw new Error('Universal runtime is unavailable.');var inputs=FTN.HeadspaceInputContext||{},context={attachments:(inputs.attachments||[]).slice()},route=FTN.UniversalRouter.route(text,context),result=await FTN.IbisRuntime.ask(text,context);if(FTN.HeadspaceRequestState&&!FTN.HeadspaceRequestState.active(token))return;renderResult(result,route,text);}catch(err){if(FTN.HeadspaceRequestState&&!FTN.HeadspaceRequestState.active(token))return;renderResult({success:false,status:'FAILED'},null,text);}
  });
})(typeof window!=='undefined'?window:globalThis);
