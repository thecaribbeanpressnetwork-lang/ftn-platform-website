// FTN Platform — ibis.ai functional intelligence workspace.
(function(global){'use strict';
  function loadScript(src){return new Promise(function(resolve){if(document.querySelector('script[src="'+src+'"]')){resolve();return;}var s=document.createElement('script');s.src=src;s.onload=resolve;s.onerror=resolve;document.head.appendChild(s);});}
  function esc(s){return String(s||'').replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
  function country(){return global.FTN&&global.FTN.Country&&global.FTN.Country.get?global.FTN.Country.get().name:'Trinidad & Tobago';}
  function hash(s){var h=2166136261;for(var i=0;i<s.length;i++){h^=s.charCodeAt(i);h=Math.imul(h,16777619);}return h>>>0;}
  function wrap(ctx,text,x,y,maxWidth,lineHeight,maxLines){var words=String(text).split(/\s+/),line='',lines=[];for(var i=0;i<words.length;i++){var test=line?line+' '+words[i]:words[i];if(ctx.measureText(test).width>maxWidth&&line){lines.push(line);line=words[i];if(lines.length>=maxLines-1)break;}else line=test;}if(line&&lines.length<maxLines)lines.push(line);lines.forEach(function(l,n){ctx.fillText(l,x,y+n*lineHeight);});return y+lines.length*lineHeight;}
  async function ensureData(){await loadScript('/js/ftn-media-discovery.js');if(!global.FTN.Auth)await loadScript('/js/ftn-auth.js');if(!global.FTN.Sources)await loadScript('/js/source-registry.js');if(!global.FTN.DataSource)await loadScript('/js/data-source.js');if(!global.FTN.indicators)await loadScript('/js/indicators-data.js');if(!global.FTN.Relationships)await loadScript('/js/relationships-data.js');}
  function numericHistory(i){return(i&&Array.isArray(i.history)?i.history:[]).map(Number).filter(Number.isFinite);}
  function change(i){var s=numericHistory(i);if(s.length<2)return null;var a=s[0],b=s[s.length-1];return{delta:b-a,pct:a?(b-a)/Math.abs(a)*100:null};}
  function relevantIndicators(q){var terms=q.toLowerCase().split(/[^a-z0-9]+/).filter(function(x){return x.length>2;});return(global.FTN.indicators||[]).map(function(i){var hay=(i.title+' '+i.category+' '+(i.changeLabel||'')).toLowerCase(),score=terms.reduce(function(n,t){return n+(hay.indexOf(t)>=0?1:0);},0);return{i:i,score:score,c:change(i)};}).filter(function(x){return x.score>0||x.c;}).sort(function(a,b){return b.score-a.score||Math.abs((b.c&&b.c.pct)||0)-Math.abs((a.c&&a.c.pct)||0);}).slice(0,8);}
  async function localAI(prompt){if(!('LanguageModel' in global))return null;try{var opts={expectedInputs:[{type:'text',languages:['en']}],expectedOutputs:[{type:'text',languages:['en']}]};var availability=await global.LanguageModel.availability(opts);if(availability==='unavailable')return null;var session=await global.LanguageModel.create(opts);var answer=await session.prompt('You are ibis.ai, FTN Platform’s Caribbean-first intelligence assistant. Be practical, concise and transparent. Do not invent current facts. When the user asks for an FTN action, connect the answer to the appropriate FTN tool. User country context: '+country()+'.\n\nUser request: '+prompt);try{session.destroy();}catch(e){}return answer;}catch(e){return null;}}
  async function serverAI(prompt){if(!(global.FTN&&global.FTN.Auth))return{available:false,reason:'FTN Account did not load.'};try{var user=await global.FTN.Auth.getVerifiedUser();if(!user)return{available:false,guest:true,reason:'Sign in to use the protected server AI route.'};var result=await global.FTN.Auth.invoke('ibis-query',{prompt:prompt,country:country()});if(!result||!result.answer)return{available:false,reason:'The server returned no answer.'};return{available:true,answer:result.answer,provider:result.provider||'Configured provider',model:result.model||'',generatedAt:result.generatedAt||new Date().toISOString()};}catch(e){return{available:false,reason:e.message||'The protected server AI route is unavailable.'};}}
  function routeResults(goal){var matches=global.FTN&&global.FTN.IntentRouter?global.FTN.IntentRouter.route(goal):[];if(!matches.length)return'<p>No strong FTN route matched. Try describing the outcome, not the product name.</p>';return'<div class="ibis-action-grid">'+matches.slice(0,5).map(function(m){return'<a class="ibis-action" href="'+esc(m.product.route)+'"><strong>'+esc(m.product.name)+'</strong><span>'+esc(m.product.tagline)+'</span><small>'+esc(m.explanation)+'</small></a>';}).join('')+'</div>';}
  function renderAnalysis(q){var rows=relevantIndicators(q);if(!rows.length)return'<p>No matching indicator history is loaded yet.</p>';return'<div class="ibis-data-list">'+rows.map(function(x){var c=x.c,p=c&&c.pct!=null?((c.pct>=0?'+':'')+c.pct.toFixed(1)+'%'):'history loaded';return'<article><strong>'+esc(x.i.title)+'</strong><span>'+esc(x.i.category)+'</span><b>'+p+'</b><small>'+esc(x.i.classification||'')+' · '+esc(x.i.sourceName||'')+'</small></article>';}).join('')+'</div><p class="workspace-muted">Open <a href="/observatory/">FTN Live</a> or <a href="/mission-control/demo/#correlation-engine">Mission Control</a> to inspect relationships and evidence.</p>';}
  async function renderMedia(q,out){out.innerHTML='<p>Searching FTN media discovery…</p>';var lower=q.toLowerCase(),music=/song|music|soca|reggae|dancehall|calypso|kaiso|chutney|kompa|zouk|steelpan/.test(lower),d;try{if(music){var genre=/reggae/.test(lower)?'reggae':/dancehall/.test(lower)?'dancehall':/calypso|kaiso/.test(lower)?'calypso':/chutney/.test(lower)?'chutney':/kompa|zouk/.test(lower)?'zouk-kompa':'soca';d=await global.FTN.MediaDiscovery.discover({mode:'music',genre:genre,queries:[q+' official video Caribbean',q+' official audio Caribbean'],limit:60},{force:true});}else d=await global.FTN.MediaDiscovery.discover({mode:'video',queries:[q,q+' Trinidad Tobago',q+' Caribbean'],limit:60},{force:true});var items=d.results||[];out.innerHTML='<div class="ibis-media-grid">'+items.slice(0,24).map(function(t){return'<a href="https://www.youtube.com/watch?v='+encodeURIComponent(t.videoId)+'" target="_blank" rel="noopener"><img src="'+esc(t.thumbnail||'')+'" alt=""><strong>'+esc(t.title)+'</strong><span>'+esc(t.channel||'YouTube')+'</span></a>';}).join('')+'</div><p class="workspace-muted">'+items.length+' embeddable source results found. Open music in <a href="/riddim/dj/">FTN DJ</a>, films in <a href="/screen/">FTN Screen</a>, or scheduled programming in <a href="/tv/">FTN TV</a>.</p>';}catch(e){out.innerHTML='<p>'+esc(e.message)+'</p>';}}
  async function createVisual(prompt,out){var ai=await localAI('Write a short headline of no more than 9 words for a Caribbean technology visual about: '+prompt);var title=(ai||prompt).replace(/[\n\r]+/g,' ').trim().slice(0,120),canvas=document.createElement('canvas');canvas.width=1200;canvas.height=630;var ctx=canvas.getContext('2d'),h=hash(prompt),hue=h%360;var g=ctx.createLinearGradient(0,0,1200,630);g.addColorStop(0,'#050505');g.addColorStop(.65,'hsl('+hue+' 55% 18%)');g.addColorStop(1,'#111');ctx.fillStyle=g;ctx.fillRect(0,0,1200,630);for(var i=0;i<14;i++){var x=(h*(i+3)%1100)+50,y=(h*(i+11)%530)+50,r=8+(h*(i+17)%40);ctx.strokeStyle='hsla('+((hue+i*17)%360)+' 80% 70% / .25)';ctx.lineWidth=2;ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.stroke();}ctx.fillStyle='#E10613';ctx.fillRect(72,70,96,8);ctx.fillStyle='#fff';ctx.font='800 30px Inter,Arial';ctx.fillText('ibis.ai · FTN PLATFORM',72,125);ctx.font='800 68px Inter,Arial';var bottom=wrap(ctx,title,72,230,920,78,4);ctx.font='500 24px Inter,Arial';ctx.fillStyle='#d5d7dc';wrap(ctx,'Caribbean-first intelligence · '+country(),72,Math.min(535,bottom+38),900,32,2);ctx.strokeStyle='rgba(255,255,255,.3)';ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(1000,120);ctx.quadraticCurveTo(1080,160,1040,250);ctx.quadraticCurveTo(1000,325,1080,400);ctx.stroke();ctx.fillStyle='#fff';ctx.beginPath();ctx.arc(1040,250,10,0,Math.PI*2);ctx.fill();var url=canvas.toDataURL('image/png');out.innerHTML='<div class="ibis-visual-result"><img src="'+url+'" alt="Generated FTN visual"><div><a class="btn btn-primary" download="ibis-ftn-visual.png" href="'+url+'">Download PNG</a><button type="button" class="btn btn-outline" id="ibis-new-visual">Regenerate</button></div></div>';var b=document.getElementById('ibis-new-visual');if(b)b.onclick=function(){createVisual(prompt,out);};}
  function injectStyle(){if(document.querySelector('link[data-ibis-style]'))return;var l=document.createElement('link');l.rel='stylesheet';l.href='/css/components/ibis-ai.css';l.setAttribute('data-ibis-style','true');document.head.appendChild(l);}
  async function init(){injectStyle();await ensureData();global.FTN.WorkspaceShell.init({productId:'ibis-ai',mountId:'workspace-root',accentSmallVar:'--color-ibis-on-dark',build:function(content){content.innerHTML='<section class="ibis-command"><div class="ibis-command__bird"><img src="/assets/panels/05-ibis-ai.png" alt="ibis.ai"><span id="ibis-ai-state">FTN intelligence ready</span></div><div class="ibis-command__work"><span class="workspace-kicker">Ask · Find · Analyze · Create</span><h1>What do you need done?</h1><form id="ibis-form"><textarea id="ibis-goal" rows="4" placeholder="Ask ibis in plain language — find Caribbean films, analyze what changed, help with a grant, create a visual, route me to the right FTN tool…" required></textarea><div class="ibis-mode-row"><button type="button" data-mode="ask" aria-pressed="true">ASK</button><button type="button" data-mode="find">FIND</button><button type="button" data-mode="analyze">ANALYZE FTN</button><button type="button" data-mode="visual">CREATE VISUAL</button></div><button type="submit" class="btn btn-primary">Run with ibis</button></form></div></section><section class="ibis-live-output" id="ibis-output" aria-live="polite"><div class="workspace-output"><h3>ibis is a tool, not a mascot.</h3><p>Ask for an outcome. On supported desktop Chrome, ibis can use the browser’s on-device language model. FTN routing, media search, data analysis and visual generation remain available without sending your prompt to an FTN-hosted LLM.</p></div></section><section class="ibis-shortcuts"><button data-example="Show me the biggest changes in national indicators">What changed?</button><button data-example="Find Trinidad and Caribbean movies I can watch">Find Caribbean movies</button><button data-example="Find current soca tracks, not DJ mixes">Find soca</button><button data-example="I need funding opportunities for a Caribbean technology startup">Find funding</button><button data-example="Create a visual for a Caribbean digital infrastructure campaign">Create a visual</button></section>';
      var mode='ask',form=document.getElementById('ibis-form'),input=document.getElementById('ibis-goal'),out=document.getElementById('ibis-output');content.querySelectorAll('[data-mode]').forEach(function(b){b.addEventListener('click',function(){mode=b.getAttribute('data-mode');content.querySelectorAll('[data-mode]').forEach(function(x){x.setAttribute('aria-pressed',String(x===b));});});});content.querySelectorAll('[data-example]').forEach(function(b){b.addEventListener('click',function(){input.value=b.getAttribute('data-example');if(/visual/i.test(input.value))mode='visual';else if(/changed|indicator/i.test(input.value))mode='analyze';else if(/find|movie|soca/i.test(input.value))mode='find';form.requestSubmit();});});form.addEventListener('submit',async function(e){e.preventDefault();var q=input.value.trim();if(!q)return;out.innerHTML='<div class="workspace-output"><p>Working…</p></div>';if(mode==='visual'||/create|generate|make/.test(q.toLowerCase())&&/image|visual|poster|graphic/.test(q.toLowerCase())){await createVisual(q,out);return;}if(mode==='find'||/find|search|movie|film|song|music|youtube/.test(q.toLowerCase())){await renderMedia(q,out);return;}if(mode==='analyze'||/what changed|correlat|indicator|econom|inflation|weather|pressure/.test(q.toLowerCase())){out.innerHTML='<div class="workspace-output"><h3>FTN data view</h3>'+renderAnalysis(q)+'</div>';return;}var answer=await localAI(q);if(answer){out.innerHTML='<div class="workspace-output"><span class="workspace-kicker">On-device AI</span><h3>ibis response</h3><p>'+esc(answer).replace(/\n/g,'<br>')+'</p><hr>'+routeResults(q)+'</div>';return;}var server=await serverAI(q);if(server.available){out.innerHTML='<div class="workspace-output"><span class="workspace-kicker">Authenticated server AI · '+esc(server.provider)+'</span><h3>ibis response</h3><p>'+esc(server.answer).replace(/\n/g,'<br>')+'</p><p class="workspace-muted">Model: '+esc(server.model||'configured server model')+' · Generated '+esc(server.generatedAt)+'</p><hr>'+routeResults(q)+'</div>';}else out.innerHTML='<div class="workspace-output"><span class="workspace-kicker">FTN deterministic router</span><h3>Best next actions</h3><p>'+esc(server.reason)+'</p>'+(server.guest?'<p><a href="/account/?return=%2Fibis-ai%2F">Sign in for the protected server AI route</a>. Your prompt has not been sent to that provider.</p>':'<p>No server answer was claimed. Your deterministic FTN routes remain available.</p>')+routeResults(q)+'</div>';});}});}
  document.addEventListener('DOMContentLoaded',init);
})(window);
