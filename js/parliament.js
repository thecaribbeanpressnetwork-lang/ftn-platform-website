(function(global){'use strict';var list=(global.FTN&&global.FTN.CaribbeanParliaments)||[],current='tt';
function esc(s){return String(s==null?'':s).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
function source(id){return list.filter(function(x){return x.id===id;})[0]||list[0];}
function watch(item){var mount=document.getElementById('parliament-watch');if(!mount)return;var media=item.latest?'<iframe title="Latest official '+esc(item.legislature)+' video" src="https://www.youtube-nocookie.com/embed/'+esc(item.latest.videoId)+'?rel=0&playsinline=1" allow="encrypted-media; picture-in-picture" allowfullscreen loading="eager"></iframe>':'<div class="parl-player__fallback"><div><strong>No verified embeddable video is available.</strong><p>FTN will not substitute an unofficial stream. Continue to the official legislature or verified video source.</p></div></div>';var title=item.latest?item.latest.title:'Official public-record access';var published=item.latest?item.latest.published:'No verified archive date';var original=item.latest?item.latest.url:(item.videoUrl||item.liveUrl||item.officialUrl);mount.innerHTML='<div class="parl-player">'+media+'<div class="parl-player__meta"><span class="parl-state">'+esc(item.state==='LIVE'?'LIVE':'OFFLINE · LATEST OFFICIAL ARCHIVE')+'</span><h2>'+esc(title)+'</h2><p>'+esc(item.country)+' · '+esc(item.legislature)+' · Published '+esc(published)+' · Source checked '+esc(item.verified)+'. FTN does not download, mirror or alter publisher video.</p><div class="parl-source-actions">'+(original?'<a href="'+esc(original)+'" target="_blank" rel="noopener noreferrer">Open original official source ↗</a>':'')+(item.videoUrl&&item.videoUrl!==original?'<a href="'+esc(item.videoUrl)+'" target="_blank" rel="noopener noreferrer">Official video channel ↗</a>':'')+(item.officialUrl?'<a href="'+esc(item.officialUrl)+'" target="_blank" rel="noopener noreferrer">Legislature website ↗</a>':'')+'</div></div></div>';document.getElementById('parliament-watch-status').textContent=item.state==='LIVE'?'An official live source is available.':'No current official live stream was verified at the last check; showing the latest verified official archive or an honest source fallback.';}
function card(item){return'<article class="parl-source-card"><div class="parl-source-card__meta"><span>'+esc(item.state)+'</span><span>'+esc(item.chambers)+'</span></div><h3>'+esc(item.country)+'</h3><p><strong>'+esc(item.legislature)+'</strong><br>'+esc(item.confidence)+' · Checked '+esc(item.verified)+'</p><div class="parl-source-card__actions"><button type="button" data-country="'+esc(item.id)+'">Watch / inspect</button>'+(item.officialUrl?'<a href="'+esc(item.officialUrl)+'" target="_blank" rel="noopener noreferrer">Official site ↗</a>':'<span>Official public source unavailable</span>')+(item.videoUrl?'<a href="'+esc(item.videoUrl)+'" target="_blank" rel="noopener noreferrer">Official video ↗</a>':'')+'</div></article>';}
function catalogue(){var mount=document.getElementById('parliament-catalogue');mount.innerHTML=list.map(card).join('');mount.querySelectorAll('[data-country]').forEach(function(button){button.onclick=function(){current=button.dataset.country;document.getElementById('parliament-country').value=current;watch(source(current));document.getElementById('watch-now').scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'});};});document.getElementById('parliament-catalogue-status').textContent=list.length+' Caribbean legislatures and distinct territorial assemblies recorded · '+list.filter(function(x){return x.videoUrl||x.liveUrl;}).length+' with verified official video or live destinations.';}
function init(){var select=document.getElementById('parliament-country');select.innerHTML=list.map(function(x){return'<option value="'+esc(x.id)+'"'+(x.id==='tt'?' selected':'')+'>'+esc(x.country)+'</option>';}).join('');select.onchange=function(){current=select.value;watch(source(current));};watch(source(current));catalogue();}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();})(window);
