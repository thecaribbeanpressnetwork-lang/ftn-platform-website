// ibis.ai — cross-product task handoff + provider-fabric video capture.
// Other FTN products can send a task with ?prompt=. ibis keeps the user's words intact,
// selects the most useful working mode, and runs only after its workspace is mounted.
// Recovery note (2026-09-09): this script also restores a visible route into the separately
// implemented Headspace surface. Headspace remains status-gated until deployed browser acceptance
// passes; exposing the route must never promote prototype-only controls to LIVE.
(function(global){
  'use strict';
  function chooseMode(prompt){var p=prompt.toLowerCase();if(/\b(create|generate|make|render|produce)\b/.test(p)&&/\b(video|clip|mp4|movie|animation|animate)\b/.test(p))return'visual';if(/image|visual|poster|flyer|graphic|thumbnail|social card|cover/.test(p))return'visual';if(/analy[sz]e|compare|correlat|indicator|data|trend|what changed/.test(p))return'analyze';if(/find|search|watch|movie|film|music|track|video|episode/.test(p))return'find';return'ask';}
  function loadScriptOnce(src,marker){return new Promise(function(resolve){if((marker&&document.querySelector('script['+marker+']'))||document.querySelector('script[src^="'+src.split('?')[0]+'"]')){resolve();return;}var s=document.createElement('script');s.src=src;s.async=false;if(marker)s.setAttribute(marker,'true');s.onload=resolve;s.onerror=resolve;document.head.appendChild(s);});}
  function loadHealthModules(){loadScriptOnce('/js/ibis-cloudflare-image-live.js?v=20260909.2','data-ibis-cloudflare-image-live');loadScriptOnce('/js/ibis-ltx-video-live.js?v=20260910.1','data-ibis-ltx-video-live');}
  async function ensureProviderFabric(){
    await loadScriptOnce('/js/ftn-node-registry.js?v=20260910.1','data-ibis-node-registry');
    await loadScriptOnce('/js/ibis-capability-taxonomy.js?v=20260910.1','data-ibis-capability-taxonomy');
    await loadScriptOnce('/js/ibis-provider-registry.js?v=20260909.1','data-ibis-provider-registry');
    await loadScriptOnce('/js/ibis-native-video-activation.js?v=20260910.1','data-ibis-native-video-activation');
    await loadScriptOnce('/js/ibis-eligibility.js?v=20260910.1','data-ibis-eligibility');
    await loadScriptOnce('/js/ibis-provenance.js?v=20260910.1','data-ibis-provenance');
    await loadScriptOnce('/js/ibis-client.js?v=20260910.1','data-ibis-client');
    await loadScriptOnce('/js/ibis-local-video-provider.js?v=20260910.1','data-ibis-local-video-provider');
    await loadScriptOnce('/js/ibis-provider-fabric.js?v=20260910.2','data-ibis-provider-fabric');
    return global.FTN&&global.FTN.IbisClient&&global.FTN.IbisProviderFabric;
  }
  function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return{'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
  function exposeHeadspace(){var main=document.getElementById('main');if(!main||document.querySelector('[data-ibis-headspace-entry]'))return;var entry=document.createElement('section');entry.setAttribute('data-ibis-headspace-entry','');entry.setAttribute('aria-label','ibis Headspace');entry.className='container';entry.style.cssText='padding-top:.75rem;padding-bottom:.25rem';entry.innerHTML='<div style="display:flex;gap:.75rem;align-items:center;flex-wrap:wrap;padding:.85rem 1rem;border:1px solid rgba(0,0,0,.14);border-radius:14px"><div style="flex:1 1 320px"><strong>ibis Headspace</strong><div style="font-size:.9rem;opacity:.75;margin-top:.15rem">Connected spatial interface · recovery preview. Live tools remain capability, health and permission gated.</div></div><a class="btn btn-primary btn-sm" href="/ibis-headspace-preview/" data-open-headspace>Open Headspace</a></div>';var root=document.getElementById('workspace-root');if(root&&root.parentNode===main)main.insertBefore(entry,root);else main.appendChild(entry);}
  function isVideoGenerationIntent(text){var q=String(text||'').toLowerCase();return /\b(create|generate|make|render|produce|animate)\b/.test(q)&&/\b(video|clip|mp4|movie|animation|text-to-video|text to video)\b/.test(q);}
  function appendBubble(kind,html){var conversation=document.getElementById('ibis-conversation');if(!conversation)return null;var welcome=document.getElementById('ibis-chat-welcome');if(welcome)welcome.remove();var el=document.createElement('div');el.className='ibis-msg ibis-msg--'+kind;var bubble=document.createElement('div');bubble.className='ibis-msg__bubble'+(kind==='ibis'?' ibis-msg__bubble--ibis':'');bubble.innerHTML=html;el.appendChild(bubble);conversation.appendChild(el);conversation.scrollTop=conversation.scrollHeight;return bubble;}
  function renderVideoSuccess(out,result,provenance){var r=result||{},src=r.videoUrl||('data:'+(r.mimeType||'video/mp4')+';base64,'+(r.video||''));var download=r.videoUrl?'':'<a class="btn btn-primary" download="ibis-video.'+esc(r.extension||'mp4')+'" href="'+esc(src)+'">Download video</a>';out.innerHTML='<span class="workspace-kicker">IBIS Provider Fabric · video artifact generated</span><video controls playsinline style="max-width:100%;border-radius:14px" src="'+esc(src)+'"></video><p class="workspace-muted">Provider: <strong>'+esc(provenance&&provenance.provider||r.provider||'verified route')+'</strong>'+(r.model?' · Model: '+esc(r.model):'')+' · Native text-to-video: '+(r.nativeTextToVideo?'yes':'not claimed')+'</p>'+download;}
  function renderVideoFailure(out,response){var p=response&&response.provenance,attempts=p&&Array.isArray(p.attempts)?p.attempts:[],reason=esc(response&&response.reason||'No eligible video provider returned a valid video artifact. No paid fallback was used.');out.innerHTML='<span class="workspace-kicker">IBIS Provider Fabric · video unavailable</span><p>'+reason+'</p>'+(attempts.length?'<div class="ibis-data-list">'+attempts.map(function(a){return'<article><strong>'+esc(a.providerId)+'</strong><span>'+esc(a.success?'success':'failed')+'</span><small>'+esc(a.errorType||'no artifact')+'</small></article>';}).join('')+'</div>':'<p class="workspace-muted">No eligible provider was available under the current cost/auth/provider rules.</p>');}
  function installVideoFabricHook(){var tries=0;function attach(){var form=document.getElementById('ibis-form'),input=document.getElementById('ibis-goal');if(!form||!input){if(tries++<80)setTimeout(attach,100);return;}if(form.dataset.ibisProviderFabricHook==='true')return;form.dataset.ibisProviderFabricHook='true';form.addEventListener('submit',async function(e){var q=input.value.trim();if(!isVideoGenerationIntent(q))return;e.preventDefault();e.stopImmediatePropagation();input.value='';appendBubble('user',esc(q));var out=appendBubble('ibis','<p class="ibis-msg__thinking">ibis is routing this video request through the provider fabric…</p>');var ok=await ensureProviderFabric();if(!ok){out.innerHTML='<span class="workspace-kicker">IBIS Provider Fabric unavailable</span><p>The shared provider-fabric module could not load. No provider was called.</p>';return;}var response=await global.FTN.IbisClient.request({nodeId:'ibis-ai',capability:'VIDEO_GENERATION',context:{authenticated:false},payload:{prompt:q,confirmFreeCreditUse:true,duration:6,resolution:'1280x720'}});if(response&&response.success)renderVideoSuccess(out,response.result,response.provenance);else renderVideoFailure(out,response||{});},true);}attach();}
  function apply(){var params=new URLSearchParams(location.search),prompt=(params.get('prompt')||'').trim();if(!prompt)return;var attempts=0;function tryMount(){var input=document.getElementById('ibis-goal'),form=document.getElementById('ibis-form');if(!input||!form){if(attempts++<50)setTimeout(tryMount,80);return;}input.value=prompt;input.dispatchEvent(new Event('input',{bubbles:true}));var mode=chooseMode(prompt),button=document.querySelector('[data-mode="'+mode+'"]');if(button)button.click();input.focus({preventScroll:true});form.scrollIntoView({behavior:'smooth',block:'center'});if(params.get('run')==='1'&&typeof form.requestSubmit==='function')setTimeout(function(){form.requestSubmit();},120);}tryMount();}
  function init(){loadHealthModules();exposeHeadspace();installVideoFabricHook();apply();}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})(window);
